{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open Pegasus UI in Browser",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/pegasus-ui",
      "preLaunchTask": "Start Pegasus UI"
    }
  ]
}
